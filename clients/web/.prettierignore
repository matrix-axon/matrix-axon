dist/
pnpm-lock.yaml
# Generated by `pnpm gen:api`; formatting is the generator's business.
src/api/schema.d.ts
# Bundled third-party emoji data; formatting would create a noisy generated diff.
public/emoji-picker-data-en.json
# Demo recording output (ADR 0086); Playwright writes it, nobody reads the JSON.
demo-artifacts/
# Rust crate for the native shell (ADR 0102, M-W12): cargo build output and
# generated schemas, neither of which is ours to format.
src-tauri/target/
src-tauri/gen/

# Generated by scripts/generate-thirdparty.sh (cargo-about over the shell's own
# cargo workspace) and rewritten wholesale. Mirrored in the prettier hook's
# exclude in .pre-commit-config.yaml; the two have to agree.
src-tauri/THIRDPARTY.md
