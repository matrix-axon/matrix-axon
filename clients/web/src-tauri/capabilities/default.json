{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Axon window is allowed to ask this process to do.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "websocket:default",
    {
      "identifier": "http:default",
      "comment": "Any host and any port, because the server is the user's own and is chosen at runtime (ADR 0102 \u00a7 3) \u2014 there is no build-time host to narrow this to. Note ':*' is load-bearing: 'http://*' alone means port 80, so a LAN axon on :8080 is denied.",
      "allow": [
        {
          "url": "http://*:*"
        },
        {
          "url": "https://*:*"
        }
      ]
    },
    {
      "identifier": "dialog:allow-save",
      "comment": "Ask the user where to put a downloaded attachment. The dialog is the only thing that chooses a path; nothing here can pick one on the user's behalf."
    },
    {
      "identifier": "fs:allow-write-file",
      "comment": "Write the bytes to the path the save dialog returned. Unscoped by design: the user names the destination, and pre-declaring a directory would either forbid the one they pick or grant more than the dialog did."
    },
    {
      "identifier": "opener:allow-open-url",
      "comment": "The command grant alone is scopeless, and a scopeless opener denies every URL -- that is why links did nothing at first. NOTE the pattern syntax differs from `http:default` above: the opener matches with `glob::Pattern` (default MatchOptions, so `*` spans `/`), NOT the URL Pattern standard. So `https://*` already covers any port and path, and writing `https://*:*` by analogy with the http scope silently denies every ordinary URL -- there is no literal `:` after the host to match. http(s) only, deliberately narrower than the plugin's own `allow-default-urls`, which also admits `mailto:` and `tel:`: `isExternalHref` in app.tsx passes neither.",
      "allow": [
        {
          "url": "http://*"
        },
        {
          "url": "https://*"
        }
      ]
    }
  ]
}
